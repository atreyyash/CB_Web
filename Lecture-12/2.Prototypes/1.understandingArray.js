let arr = [1, 2, 3, 4];

arr.forEach((val, idx, ar) => {
    console.log("Index : ", idx, "Value:", val, ar);
});

// Prototypes are objects that are used to create variables which have constructor to create these varibales and functions which
// can be used by a datatype.